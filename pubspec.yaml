name: solux
description: Solux — SRX Chain Wallet
publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.5.5
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  share_plus: ^10.0.0
  sensors_plus: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

# assets:
#   - assets/images/
#   - assets/icons/
